export interface CepServiceResponseProps {
  zip_code: string;
  address: string;
  address_complement: string;
  neighborhood: string;
  city: string;
  state: string;
}

export interface CepProps {
  zipCode: string;
  address: string;
  complement: string;
  neighborhood: string;
  city: string;
  state: string;
}
